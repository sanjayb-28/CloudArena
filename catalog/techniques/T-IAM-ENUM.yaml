id: T-IAM-ENUM
name: Enumerate IAM Roles
provider: aws
requires:
  services:
    - iam
safety: read_only
severity: low
impl:
  adapter: sdk.iam.enum
mitre:
  - TA0003
planner:
  auto: true
  priority: 60
  rule: simple_auto
report:
  summary_rule: generic
